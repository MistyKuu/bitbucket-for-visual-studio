<UserControl x:Class="GitClientVS.UI.Views.PullRequestDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:converters="clr-namespace:GitClientVS.UI.Converters"
             xmlns:styles="clr-namespace:GitClientVS.UI.Styles"
             xmlns:viewModels="clr-namespace:GitClientVS.Infrastructure.ViewModels;assembly=GitClientVS.Infrastructure"
             xmlns:gitClientModels="clr-namespace:GitClientVS.Contracts.Models.GitClientModels;assembly=GitClientVS.Contracts"
             xmlns:controls1="clr-namespace:GitClientVS.UI.Controls"
             xmlns:ui="clr-namespace:GitClientVS.UI"
             xmlns:bindings="clr-namespace:GitClientVS.UI.Bindings"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance viewModels:CreatePullRequestsViewModel,IsDesignTimeCreatable=False}" 
             >
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <styles:SharedResourceDictionary Source="pack://application:,,,/GitClientVS.UI;component/Styles/SharedDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <DataTemplate x:Key="FilesChangedTemplate">
                <TextBlock Text="{Binding Path}"/>
            </DataTemplate>
            <DataTemplate x:Key="CommitsTemplate" >
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <controls1:CustomizedListItem
                    Date="{Binding Updated}"
                    Title="{Binding Title}"
                    Sender="{Binding Author.DisplayName}"
                    Image="{Binding Author.Links.Avatar.Href}"
                    />
                    <Separator Grid.Row="1"/>
                </Grid>
            </DataTemplate>
            <DataTemplate x:Key="CommentsTemplate" >
                <TextBlock Text="{Binding Text}"/>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Vertical">
            <Expander Header="Files Changed">
                <ListBox ItemsSource="{Binding FilesChanged}" ItemTemplate="{StaticResource FilesChangedTemplate}"/>
            </Expander>
            <Expander Header="Commits">
                <ListBox ItemsSource="{Binding Commits}" ItemTemplate="{StaticResource CommitsTemplate}"/>
            </Expander>
            <Expander Header="Comments">
                <ListBox ItemsSource="{Binding Comments}" ItemTemplate="{StaticResource CommentsTemplate}"/>
            </Expander>
        </StackPanel>
    </Grid>
</UserControl>
